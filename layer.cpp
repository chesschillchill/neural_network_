#include "layer.h"

//virtual vector<float> calculateValues(const vector<float>& inputs) {
//	vector<float> values;
//
//	for (int i = 0; i < nodes.size(); ++i) {
//		values.push_back(nodes[i].calculateValue(inputs));
//	}
//
//	return values;
//}

//vector<float> Layer::activationLayer(const vector<float>& values){
//	return ActivationFunction::relu(values);
//}
//
//vector<float> Layer::derActivationLayer(const vector<float>& values){
//	return ActivationFunction::reluDerivative(values);
//}